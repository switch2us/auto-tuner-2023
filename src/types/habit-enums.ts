export enum HabitGoalUnitType {
    TIMES,
    MINUTES,
    HOURS,
    PAGES,
    KILOMETERS
}

export enum HabitTimeOfDay {
    EARLY_MORNING,
    MORNING,
    AFTERNOON,
    LATE_AFTERNOON,
    EVENING,
    NIGHT
}

export enum HabitRepeatingWeekDays {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY
}

export enum HabitRepeating {
    DAILY,
    EVERY_TWO_DAYS,
    EVERY_THREE_DAYS,
    EVERY_FOUR_DAYS,
    EVERY_FIVE_DAYS,
    EVERY_SIX_DAYS,
    WEEKLY,
    EVERY_TWO_WEEKS,
    EVERY_THREE_WEEKS,
    EVERY_FOUR_WEEKS,
    MONTHLY,
    QUARTERLY,
    HALF_YEARLY,
    YEARLY
}